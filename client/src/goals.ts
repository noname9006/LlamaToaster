// Same re-export-point convention as client/src/types.ts and
// client/src/vramEstimate.ts -- BENCHMARKING_PLAN_V8.md's shared logic the
// client needs at runtime (M2's questionnaire, M3's scoring shapes, M4's KV
// pruning, §0.6's ETA pricing, N1/N5's derived reads, N7's bundle format).
export * from "../../shared/goals.js";
