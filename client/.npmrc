# See ../.npmrc -- same fix, needed here too since npm reads project config
# from the current working directory, not the parent, when someone `cd
# client && npm install` directly instead of going through the root postinstall.
audit=false
